# DevBox v0.1.9 release gate

Bir v0.1.9 Windows kurucusu yalnız aşağıdaki gerçek kontroller başarılıysa teslim edilebilir:

1. sohbet rich activity payload regresyon testi PASS;
2. Codex resmi Windows standalone executable discovery regresyon testi PASS;
3. `pnpm evolution:verify` PASS;
4. TypeScript typecheck PASS;
5. unit/integration test paketi PASS;
6. production build + truth audit PASS;
7. Windows NSIS `DevBox-Setup.exe` gerçek dosyası üretilmiş;
8. PE `MZ` başlığı ve minimum boyut kontrolü PASS;
9. SHA-256 yeniden hesaplaması artifact checksum ile birebir eşleşmiş.

Bu kapılardan biri başarısızsa sürüm READY/PASS olarak raporlanmaz.
